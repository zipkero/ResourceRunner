# resource-visualization

## 요약

이미 동작하는 CPU·Memory 카드와 상세 팝업이 값을 보여주는 방식을 바꿉니다.
상세 순위 정원 확장과 CPU 상세 앱 순위 섹션, Memory 구성 항목 시각화,
CPU 그래프의 격자와 User·System 2계열, 목록의 앱 아이콘을 다룹니다.

## 상태

- [x] SPEC
- [x] ANALYSIS
- [ ] IMPLEMENT

## 문서

- [spec.md](./spec.md)
- [analyze.md](./analyze.md) (ANALYSIS 단계에서 생성)
- [implement.md](./implement.md) (IMPLEMENT 단계에서 생성)

## 작업 히스토리

- 2026-08-18: SPEC 작성.
  사용자가 실행 중인 앱에서 보고한 세 문제 중 core-resource-monitoring이 결함으로 처리하지 못한 나머지를 담았습니다 —
  상세 순위 개수 상한과 Memory 그래프는 `docs/product.md` 요구사항을 바꾸므로 새 feature로 분리했습니다.
  참고 앱 스크린샷(CleanMyMac Memory·CPU 패널, iStat Menus 드롭다운)에서 설계 입력을 얻었습니다.
  범위에서 뺀 것 — 프로세스 종료(Quit) 버튼은 읽기 전용 모니터링을 넘어서고,
  그래프 시간 범위 선택은 설정 UI가 필요해 M4 소관입니다.
  Memory 카드 높이를 늘리는 방향도 접고 기존 텍스트 줄을 그래프가 대체하도록 했습니다 —
  본체 팝오버 높이가 두 카드 기준 실측값이고 M3에서 카드가 넷으로 늘어나기 때문입니다.
  승인 전 확인 한 건이 남아 있습니다 — 누적 바 색 구간 합과 「사용 중」 수치의 관계.
  둘이 같은 정의가 아니라(task-001에서 Activity Monitor 공식으로 확정) 화면에서 어긋나 보일 수 있습니다.
- 2026-08-18: SPEC 재작성. 승인 전 확인 한 건에 답을 받아 반영했습니다 —
  누적 바 구간 합과 「사용 중」 수치를 별개 지표로 분리합니다.
  구간 합을 「사용 중」에 맞추는 두 방식은 접었습니다 —
  「사용 중」 정의를 되돌리면 Activity Monitor 편차 0 일치가 깨지고(task-001 결정),
  「사용 중」을 바의 채운 길이로 삼으면 구간 길이가 실제 바이트에 비례하지 않아 값이 왜곡됩니다.
  §3에 값 왜곡 금지와 두 지표 구분을, §4에 「사용 중」 정의 변경 제외를, §5.3에 비례·구분 요구를 더했습니다.
- 2026-08-18: ANALYSIS 작성.
  표시 계층 안에서 끝나는 변경을 원칙으로 하고 예외 하나를 확정했습니다 —
  CPU 그래프 2계열은 시각별 User 값을 요구하므로 이력 항목에 필드 하나가 늘어납니다.
  Memory 구성 시각화는 최신 스냅샷만 쓰므로 이력 계약을 요구하지 않습니다.
  상세 순위와 전체 프로세스 목록을 목록 하나로 통합했습니다 —
  같은 순위가 두 번 나타나지 않는 것을 표시 값 모델에서 순위 필드를 하나만 남겨 보장하고,
  정원은 순서 고정과 같은 순수 함수 안에서 걸어 펼친 행이 있는 동안 목록 구성이 흔들리지 않게 했습니다.
  Memory 상세에 이미 있던 「현재 사용량 순위」와 전체 목록의 중복도 이 통합으로 함께 해소됩니다.
  Memory 카드는 Pressure·Swap 줄을 합쳐 구성 범례 자리를 만들고 잘림 우선순위를 서식 순서로 고정했습니다.
  Decision Point 16개를 확정했고 남은 미해결 결정은 없습니다.
  통합의 대가를 §4에 기록했습니다 — 상세 목록이 전체에서 정원까지로 줄어 정원 밖 앱은 상세에 나타나지 않습니다.
- 2026-08-21: IMPLEMENT 체크리스트 작성.
  Task 13개를 5개 Section으로 나누고 SPEC §5.1~§5.10 전체를 매핑했습니다(미매핑 없음).
  순서는 의존 관계를 위치로 표현했습니다 — 정원 상수 분리가 목록 통합의 전제이고,
  Memory 카드 줄 병합이 구성 범례 자리의 전제이며, 아이콘 캐시가 행 아이콘 표시의 전제입니다.
  검증은 순수 함수 단위 테스트와 렌더 높이 측정을 축으로 두고 UI 테스트는 늘리지 않으며,
  각 확인 항목에 「무엇을 되살리면 어떤 단언이 실패해야 하는가」를 적었습니다.
- 2026-08-21: task-001~003 완료. `SPEC §5.1`(task-002 승인)·`§5.2`(task-003 승인)가 닫혔습니다.
  정원을 `cardDisplayCount`(5)·`detailCount`(20) 두 상수로 나누고,
  `MemoryCardDetail.currentUsageRanking`을 없애 `applications`가 현재 사용량 순위를 겸하게 했습니다.
  상세에서 같은 지표의 순위가 두 번 나오지 않고, 목록이 20개까지 나옵니다.
  머리글·caption은 `assemble`이 조립 시점에 완성해 필드로 넘기고 뷰는 소비만 합니다(DP2·DP15).
  **`SPEC §5.2` 해석을 확정했습니다** — 「상세 상위 5개가 같은 시점 카드 순위와 일치」는 **정상 갱신 상태 기준**입니다.
  `displayedGroups`가 펼친 행이 있는 동안 상세 순서를 고정하므로 그 동안에는 카드와 갈릴 수 있는데,
  이 고정은 core-resource-monitoring에서 매초 재정렬이 클릭 대상을 옮기는 것을 막으려고 승인된 장치이고
  analyze.md DP2가 그 위에 정원 자르기를 얹은 것이라, 모순이 아니라 예외로 봅니다. 사용자 결정입니다.
  **회귀 그물에서 배운 것 셋** —
  (1) task-001이 두 번 반려됐습니다. `label.contains(상수)` 자기참조 단언은 문자열을 고정하지 못하고,
  뷰 본문에서 정원을 고르면 단위 테스트가 닿지 못해 UI 테스트에 의존하게 됩니다.
  (2) `Mirror` 자식에 `value is [T]`를 쓰면 **빈 배열이 원소 타입과 무관하게 참**이 됩니다.
  선언 타입 비교(`type(of:) == [T].self`)로 바꿔야 합니다.
  (3) `#expect`는 실패해도 중단하지 않으므로 그 뒤 방어 없는 인덱스 접근이 trap하고,
  크래시 후 러너 재시작으로 실행되지 않은 테스트까지 실패로 찍혀 실패 건수가 부풀려집니다(실측 3건 → 269건).
  길이는 `try #require`로 먼저 보장합니다.
- 2026-08-21: task-004 완료. `SPEC §5.5`가 닫혔습니다.
  CPU 그래프가 User(아래)·System(위) 2계열이 됐고, 위 밴드를 `max(0, 전체 − User)`로 유도해
  **두 밴드 합 = 전체 사용률이 계산 구조에서 나옵니다.**
  「같은 시점」은 세 겹으로 보장됩니다 — 저장소가 한 tick의 `overallUsage`와 `userRatio`를 같은 항목에 담고,
  `assemble`이 그것을 한 점으로 옮기며, 다운샘플링이 점을 **구조체 통째로** 골라 짝이 갈라질 경로가 없습니다.
  세 자리 각각에 대응하는 mutation과 합 파괴 mutation까지 다섯 건이 모두 잡힙니다.
  색 팔레트를 `DashboardColorPalette.swift` 한 자리로 모았습니다 —
  User `#2a78d6`/`#3987e5`, System `#eb6834`/`#d95926`(라이트/다크).
  `NSColor(name:dynamicProvider:)`로 다크를 자동 반전이 아니라 별도 값으로 씁니다.
  값은 dataviz 검증기로 고른 것이고 색맹 시뮬레이션 ΔE 24.7(라이트)·26.8(다크)로 기준(≥8)을 크게 넘습니다.
  라이트에서 주황이 배경 대비 3:1 아래라 「수치 라벨이 보여야 한다」 조건이 붙었고, 요약 둘째 줄의 수치가 그 역할을 합니다.
  신규 파일이 `project.pbxproj` 수정 없이 빌드에 들어갑니다 — Xcode 동기화 파일 시스템 그룹을 쓰기 때문이며,
  pbxproj는 M1 기준(`0dfba14`)과 여전히 바이트 동일합니다.
  **남은 위험 — 색 외 구분 수단에 회귀 그물이 없습니다.**
  채움 밀도(0.5/0.28)를 둘 다 0.4로 맞추고, 경계선을 둘 다 실선으로 바꾸고, 요약 줄 스와치 둘을 지우는 mutation이
  299개를 전부 통과합니다. 계획상 스와치 소멸은 task-012의 접근성 단언이 덮지만
  **밴드 스타일 자체는 그 뒤에도 그물 밖에 남습니다.** task-012 시점에 이 자리를 다시 봐야 합니다.
  카드 높이는 implementer가 「변경 전후 직접 대조가 없다」고 한계를 밝혔으나 실제로는 메워져 있었습니다 —
  기존 `cpuCardHeightIsSameAcrossAllFourStates`가 재는 세 상태는 `cached == nil`이라 스와치 없는 `Text("–")`로 그려져,
  「스와치 있는 렌더 vs 없는 렌더」 대조가 이미 성립합니다(스와치를 8→40pt로 키운 mutation이 이 테스트에서 잡힘).
- 2026-08-21: task-005 완료. `SPEC §5.6`이 닫혔습니다.
  CPU 그래프에 25·50·75% 기준선이 깔렸고, 값→세로 좌표 변환(`HistoryGraphGridline.yPosition(forValue:height:)`)을
  뷰 밖 순수 함수로 뒀습니다. 60pt에서 45·30·15pt로 15pt 간격입니다.
  **그리기 순서와 불투명도를 단위 테스트로 잡는 방법을 찾았습니다** —
  `DrawLayer` 열거형과 `drawOrder` 배열을 두고 `Canvas` 본문이 그 배열을 그대로 순회하게 했습니다.
  `context.fill`·`context.stroke` 호출이 전부 그 `switch` 안에만 있어 우회 경로가 없으므로,
  배열을 단언하는 것이 중복 선언이 아니라 실제 그리기를 지배하는 단일 소스를 단언하는 것이 됩니다.
  격자를 밴드 뒤로 재배열하는 mutation과 `fillOpacity`를 1.0으로 바꾸는 mutation이 모두 잡힙니다.
  좌표 변환 mutation 4종(위아래 뒤집기, 목록 비우기, 50%만 남기기, 고정 좌표 반환)도 전부 잡힙니다.
  **implement.md의 확인 항목 하나를 정정했습니다** — 「값 없을 때 격자를 그리지 않는 mutation이
  **높이 단언에서** 실패해야 한다」는 성립할 수 없는 조건이었습니다.
  자리표시 높이는 `Canvas`의 그리기 내용이 아니라 `.frame(height: 60)`이 정하므로 격자 유무가 렌더 높이를 바꾸지 않고,
  실제로 격자 호출만 지운 mutation에서 306개가 전부 통과했습니다(그물 0).
  값 있는 경로에서 작동을 입증한 `drawOrder` 방식을 자리표시에도 적용하는 것으로 수단을 바꿨습니다 —
  `placeholderDrawOrder` 상수를 뷰 밖 타입에 두고 `Canvas`가 순회하게 했고,
  배열을 비우는 mutation이 단위 테스트 1건을 깨끗하게 실패시키는 것을 확인했습니다.
  상수를 뷰 밖에 둔 덕에 `GraphPlaceholderView`는 `private`로 남습니다.
  검증 조건을 약화한 것이 아니라 0이던 그물을 작동하는 것으로 바꾼 것이며, Task 목적은 그대로입니다.
  **task-011에도 같은 형태의 조건이 있습니다**(「값이 없을 때 그래프·구성 바·스와치·아이콘 중 하나라도
  그리지 않게 하면 실패해야」). 그 시점에 이 레이어 목록 앵커를 재사용할 수 있는지 봐야 합니다.
  **남은 위험** — `drawCPUGraphGridlines` **본문이 비는** mutation은 값 있는 경로·자리표시 양쪽에서 여전히 통과합니다.
  SwiftUI `Canvas`의 그리기 내용 자체를 단위 테스트로 단언할 수단이 없어서이고,
  ANALYSIS §5 DP15 채택안이 미리 받아들인 「뷰 본문 안에서만 성립하는 실수는 남는다」에 해당합니다.
- 2026-08-21: task-006 완료. 닫히는 `SPEC §5.N`은 없습니다(§5.3·§5.8 모두 뒤 Task가 남음).
  Memory 카드의 Pressure 줄과 Swap 줄을 한 줄로 합쳐 아래에 빈 슬롯 하나를 만들었습니다 — task-007의 구성 범례가 씁니다.
  슬롯 수는 5개 그대로이고, 렌더 높이는 병합 전 181.02pt → 병합 후 181.0pt로 **줄어드는 쪽**이라
  spec.md §3의 「커지지 않습니다」에 걸리지 않습니다. 가장 긴 Pressure·Swap 조합에서도 181.0pt로 같습니다.
  잘림 순서(기호 → 라벨 → 구분자 → Swap 사용량 → 변화량)를 뷰 밖 순수 함수가 배열로 고정하고 뷰는 이어붙이기만 합니다.
  접근 필드가 요구한 mutation 네 종(순서 뒤바꿈, 기호 제거, 구분자 제거, 변화량을 앞으로)이 모두 잡힙니다.
  **표시 버그를 하나 함께 고쳤습니다** — Swap 변화량이 음수일 때 부호가 빠져
  감소분이 증가처럼 보였습니다(`change >= 0 ? "+" : ""` + `abs(change)`).
  crash가 아니라 표시 오류이고, 병합으로 `swapLine`이 사라지면서 새 순수 함수가 그 로직의 유일한 소유자가 되어
  불가피하게 다시 쓰는 자리였습니다. 부호를 되돌리는 mutation이 회귀 테스트에서 잡힙니다.
  **implement.md 확인 항목을 또 하나 정정했습니다** — 「줄 수 제한을 지우는 mutation을 렌더 높이 단언이 잡아야 한다」는
  성립할 수 없습니다. `ByteCountFormatter`가 어떤 바이트 값도 짧은 문자열로 압축해
  병합 줄이 콘텐츠 폭(248pt)을 넘기지 못하므로(실측 176pt) 줄바꿈 자체가 일어나지 않습니다.
  task-005에서 확립한 앵커 방식으로 바꿨습니다 — 줄 수 상한을 뷰 밖 상수(`maximumLineCount`)로 두고
  뷰가 그것만 쓰게 한 뒤 상수를 단언합니다. 상수를 2로 바꾸는 mutation이 잡히고, 뷰에 하드코딩된 `1`은 없습니다.
  **verify가 `style/minor`로 한 번 반려했습니다** — `DashboardCardLayoutTests`의 docstring이
  「`lineLimit(1)`을 지우는 mutation이 이 테스트에서 높이 증가로 잡힌다」고 적었는데 실증적으로 거짓이었습니다
  (`.lineLimit` 제거 후 313개 전부 통과). 같은 커밋의 implement.md가 정반대를 명시한 상태라 모순이었고,
  그대로 두면 실제 그물인 `maximumLineCountIsOne()`을 중복으로 오인해 지울 위험이 있었습니다.
  주석을 사실대로 고치고 오독을 부르던 변수명 `baselineHeight`를 `mergedDefaultHeight`로 바꿨습니다.
  **남은 한계** — 「렌더 높이가 병합 전 실측값과 같다」를 단언하는 테스트는 저장소에 없습니다.
  위 181.02 → 181.0 대조는 verify가 병합 전 뷰를 임시 복원해 잰 값이며 회귀 그물로 남지 않았습니다.
  DP4가 「잘림보다 먼저」 적용하라고 한 글자 크기 축소는 구현하지 않았습니다 —
  이 줄이 어떤 값에서도 콘텐츠 폭을 넘기지 않아 축소·잘림 어느 쪽도 발동하지 않으므로 관찰 가능한 차이가 없습니다.
- 2026-08-21: **task-007 보류 — 사용자 결정이 필요합니다.** 체크박스는 `[ ]`이고 코드는 작업 트리에 남아 있습니다.
  누적 바·구간 계산·범례 조립·색은 요구를 채웠고 mutation 12종이 전부 잡힙니다(verify 확인).
  걸린 것은 **카드에 구성 수치를 둘 자리가 있는가** 하나입니다.
  **실측** — 스와치+이름+수치 네 쌍은 347pt(최장 375pt)가 필요한데 카드 콘텐츠 폭은 232pt입니다.
  담으려면 글자가 6.2~6.7pt로 줄어 읽을 수 없습니다.
  스와치+이름만이면 219pt로 축소 없이 들어갑니다(여유 13pt).
  정상 상태 제목 줄에는 약 96pt가 남아 있어 **짧은 합계 하나를 놓을 여지는 있습니다**(바가 짧아지는 대가).
  **main이 판단을 잘못했습니다.** `SPEC §5.4`가 상세에만 「각 항목의 수치가 범례로 함께 보입니다」를 두는 것을 근거로
  「카드는 이름까지, 수치는 상세·접근성으로」라고 정하고 implement.md만 고쳤습니다. 두 가지가 틀렸습니다.
  ① `SPEC §5.3`의 셋째 문장이 「**구성 합계**와 「사용 중」 수치는 서로 다른 지표로 구분되어 읽힙니다」이고
  §5.3은 통째로 카드 조건입니다. 지금 카드에는 「사용 중」만 있어 비교 대상이 없습니다.
  실측이 무너뜨리는 것은 DP4의 「이름과 **수치**를 함께」까지이고, 「수치를 전부 카드 밖으로」는 그보다 넓은 결론입니다.
  ② `ANALYSIS §5 DP4`가 「바만 그리고 항목 수치는 카드에 두지 않음」(옵션 C)을 **명시적으로 접고**
  옵션 D를 **사용자 확인 아래** 채택했는데, implement.md만 고쳐 그 결정을 뒤집었습니다.
  CLAUDE.md가 정한 순서(spec.md → analyze.md → implement.md)의 반대이고,
  「analyze.md가 확정한 결정을 바꾸는 변경은 멈추고 확인받는다」에도 걸립니다.
  그래서 implement.md task-007을 **원문으로 되돌려** analyze.md DP4와 다시 일치시켰습니다.
  **푸는 길 두 가지 — 어느 쪽이든 analyze.md 재작성이 필요합니다.**
  ① `SPEC §5.3`을 그대로 두고 카드에 **구성 합계 수치 하나**를 놓는 배치로 DP4를 실측(232 / 219 / 375)과 함께 다시 씁니다.
  `/analyze-init`만 있으면 되고 사용자가 승인한 완료 조건을 건드리지 않습니다.
  ② 「카드에는 구성 수치를 두지 않는다」를 확정하려면 `§5.3`의 첫·셋째 문장을 먼저 고쳐야 합니다 —
  `/spec-init` 뒤 `/analyze-init`. 사용자가 승인한 완료 조건의 뜻을 바꾸므로 확인 없이 진행하지 않습니다.
  verify는 ①을 권했습니다. 코드는 어느 쪽이든 `MemoryCompositionLayout`·`MemoryCompositionLegendFormatting`·
  `MemoryCompositionBarView`가 대부분 살아남습니다.
  **덧붙은 한계** — 카드의 **값 없음 상태**에서 구간을 0으로 그리지 않는 것은 어떤 테스트도 덮지 않습니다.
  「전체 물리 메모리 0」 입력은 `zeroTotalPhysicalMemoryProducesNoSegments()`가 잡지만,
  뷰의 `cached?.compositionLayout.segments ?? []` 경로는 값 없음에서 길이 0 구간 넷을 그리거나
  지어낸 20% 구간 넷을 그리는 mutation이 320개를 전부 통과합니다.
  접근 필드의 mutation 목록에 이 경우가 없어 verify는 반려 사유로 세지 않고 한계로 적었습니다.
- 2026-08-21: task-009 완료. 닫히는 `SPEC §5.N`은 없습니다(§5.9는 task-013, §5.7은 task-010이 남음).
  순서를 이탈해 task-007·008보다 먼저 했습니다 — 007이 사용자 결정 대기로 보류됐고 008이 같은 라인에 걸리며 009는 독립적이기 때문입니다.
  앱 키 → 표시용 이미지(또는 `nil`) 계약 `ApplicationIconProviding`과 유일한 구현인 고정 상한 512 LRU `ApplicationIconCache`를 만들었습니다.
  시스템 호출은 주입 가능한 로더 클로저 하나에만 남고, 소유자는 `ApplicationCoordinator` 한 곳이며
  `DashboardView`는 저장만 합니다(사용은 task-010). mutation 9종이 잡힙니다.
  **`SPEC §5.9`(자기 CPU 사용량)가 걸린 자리라** 「캐시가 채워진 뒤 호출 0」이 핵심이고,
  가짜 로더를 주입해 호출 키를 세는 구조라 task-013의 자동 검증이 여기 기댈 수 있습니다.
  **원본 비참조를 실측으로 확인했습니다** — 원본에 `weak` 참조를 걸고 관찰한 결과
  `NSBitmapImageRep`에 직접 그리는 현재 구현은 원본이 해제되고(`sourceStillAlive=false`),
  `NSImage(size:flipped:drawingHandler:)` 대조군은 클로저가 원본을 붙듭니다(`true`).
  캐시 이미지는 32×32 비트맵 표현 1개이고 Safari 원본은 표현 32개입니다.
  **task-010으로 넘기는 실측 사실 둘 (Sandbox 켜진 테스트 호스트에서 측정)**
  ① `NSWorkspace.shared.icon(forFile:)`는 **한 번도 `nil`을 돌려주지 않습니다** —
  없는 `.app` 경로, `/nonexistent/abcdef`, 빈 문자열, `"not a path at all"` 8개 입력 전부
  32×32pt·표현 32개 일반 아이콘을 반환했고 축소도 전부 통과했습니다.
  그러면 `ANALYSIS §5 DP12`가 정한 「아이콘을 얻지 못한 행의 중립 기호」가 **화면에 나타나지 않습니다.**
  ② 같은 환경에서 `FileManager.default.fileExists`는 `/Applications/Safari.app`을 `true`,
  없는 경로를 `false`로 정확히 구분합니다. 로더에 경로 존재 확인을 넣으면 중립 기호가 살아납니다.
  implementer가 이 확인을 「Sandbox 허용 여부가 측정된 적 없다」며 추정으로 넣지 않고 task-010 결정 항목으로 남긴 판단은 타당했고,
  그 전제가 이번 verify에서 측정됐습니다. **task-010에서 DP12를 어떻게 살릴지 정해야 합니다.**
  **캐시 이미지 크기 16pt(2x)를 이 Task에서 확정했으므로**, task-010이 행 아이콘을 그보다 크게 잡으면 흐려집니다 —
  `displayPointSize`를 함께 올려야 합니다.
  **주석 근거 하나를 정정했습니다** — 「첨자 대입은 `nil`을 항목 제거로 해석할 여지가 있어 `updateValue`를 쓴다」가
  이 표현식에는 성립하지 않습니다. `loaded`가 변수라 첨자 대입도 `.some(loaded)`로 승격되며,
  verify가 `cache[key] = loaded` mutation으로 7/7 통과를 확인했습니다. 리터럴 `nil` 대입에서만 제거가 일어납니다.
  코드는 어느 쪽이든 옳지만 주석이 든 이유가 틀려서, task-006 반려 사유였던 「거짓 근거를 적는」 패턴을 피하려 문구를 고쳤습니다.
  **범위 밖 발견** — `DashboardView.swift:541`에 Swift 6 모드에서는 오류가 될 경고가 있습니다
  (`main actor-isolated conformance of 'HistoryGraphView.BandRole' to 'Equatable'`). task-004·005 코드이고 현재 Swift 5라 빌드는 통과합니다.
- 2026-08-21: task-010 완료. **`SPEC §5.7`이 닫혔습니다**(매핑이 task-009·010 둘뿐).
  카드 순위·상세 앱 목록(CPU·Memory)·최근 증가량 순위 세 행 종류 앞에 고정 크기 아이콘 자리가 생겼습니다.
  그릴 내용(`.icon`/`.missing`/`.reserved`)·크기(카드 12pt·상세 16pt)·감춤 여부·줄별 대상이 모두
  `ApplicationRowIcon.swift`의 상수와 순수 함수라 뷰 본문이 값을 고르지 않습니다. mutation 8종이 잡힙니다.
  **「아이콘 있는 줄에만 자리가 생기는」 형태가 구조적으로 불가능합니다** —
  `CardRankingSlotView`의 `ForEach`가 `cardRowIconKeys` 결과를 순회하는데 그 함수가 `(0..<capacity).map`이라
  언제나 정원 길이를 돌려주고 앱 없는 줄만 `nil`입니다. 그리는 줄 수와 아이콘 대상이 같은 배열에서 나옵니다.
  카드 높이는 아이콘 전과 같은 **CPU 243.0 / Memory 181.0**이고, 아이콘 뷰를 제거하는 mutation이 통과하는 것으로 실증됐습니다.
  **DP12를 살리려고 task-009 코드를 고쳤습니다**(main 승인) —
  `loadSystemIcon(for:)`에 `FileManager.fileExists` 확인을 넣어 없는 경로에 `nil`을 돌려줍니다.
  없으면 `icon(forFile:)`가 일반 문서 아이콘을 줘 「아이콘 없음」이 드러나기는커녕 있는 것처럼 보이고,
  core `SPEC §5.11`의 「값을 지어내지 않는다」에 걸립니다. `ANALYSIS §5 DP11` 다섯 항목은 그대로라 analyze.md 재작성이 필요 없었습니다.
  **verify가 샌드박스 probe로 큰 위험을 하나 막았습니다** — 가드가 실제 앱 경로를 막으면 모든 행이 중립 기호가 됩니다.
  `TEST_HOST`가 `ResourceRunner.app`이고 `ENABLE_APP_SANDBOX = YES`인 상태에서 직접 재보니
  `/Applications/Ghostty.app`·`/System/Applications/Calculator.app`·`/sbin/launchd`·`/usr/libexec/logd`와
  실제 실행 중 프로세스에서 유도한 키까지 전부 `exists=true, loader=true`였습니다.
  기존 테스트는 컨테이너 안 임시 디렉토리만 써서 이 경로를 덮지 못합니다.
  **implement.md 확인 항목을 또 정정했습니다(세 번째)** — 「접근성 감춤을 **접근성 요소 수**로 확인」이 성립 불가입니다.
  `.accessibilityHidden(true)`든 아니든 `NSHostingController` 안에서 하위 요소 수가 **똑같이 1**이고,
  창 표시·레이아웃·RunLoop 대기 뒤에도 같아 SwiftUI 접근성 트리가 실체화되지 않습니다.
  task-005·006과 같은 「뷰 밖 상수 + 상수 단언」으로 바꿨습니다.
  덤으로 카드 쪽은 감싸는 `Button`의 `.accessibilityElement(children: .ignore)` 때문에 **구조적으로 이미 성립**하고,
  실제 위험은 상세 팝업 쪽뿐임이 확인됐습니다.
  **남은 위험 둘** —
  ① 중립 기호 **내용만** 비우고 자리를 남기는 mutation은 잡히지 않습니다.
  `.frame`이 크기를 정하고, 헤드리스 SwiftUI가 SF Symbol을 칠하지 않아(`.missing`과 `.reserved`가 똑같이 102픽셀)
  `ImageRenderer` 픽셀 계수도 쓸 수 없었습니다. 기호 이름 오타는 `neutralSymbolResolvesToARealSystemSymbol`이 막습니다.
  DP15 옵션 B가 받아들인 잔여 범위입니다.
  ② **행 식별자·탭 동작이 실행으로 확인되지 않았습니다.** 기기에서 XCUITest 초기화가
  `LocalAuthentication Code=-4 "System authentication is running."`으로 막힙니다(`coreauthd`·`coreautha` 상주, 3회 시도 동일).
  소스 대조로는 `.accessibilityIdentifier("AppRow-…")`·`.contentShape(Rectangle())`·`.onTapGesture`가 한 글자도 안 바뀌었고
  아이콘이 그 히트 영역 안에 들어갔습니다. **인증 창이 해소되면 `DashboardProcessListDisplayUITests`·
  `DashboardDetailExpansionUITests`를 돌려 이 자리를 닫아야 합니다.** task-011이 `DashboardDetailPopoverUITests`를
  요구하므로 그때 함께 처리할 수 있습니다.
- 2026-08-21: task-013 **자동 부분만 완료**. 체크박스는 `[ ]`입니다 —
  사람 관찰(변경 전후 앱을 Release로 띄우고 Activity Monitor에서 10분간 1분 간격 `%CPU` 10회씩 비교)이 남았습니다.
  순서 이탈 — 007이 보류고 008이 같은 라인이며 011·012는 「새 표시 요소가 **모두** 있는 상태」를 전제해
  007·008을 필요로 하므로, 지금 진행 가능한 것이 이것뿐이었습니다.
  **기존 커버리지를 먼저 조사해 빈자리 셋을 찾았습니다** — task-009의 캐시 테스트 7건은
  캐시를 직접 부르는 형태뿐이라 ① 여러 행·여러 뷰가 한 프레임에서 같은 키를 조회하는 경우,
  ② tick 반복(화면을 두 번 이상 그리는 테스트가 하나도 없었습니다), ③ **캐시 수명**이 비어 있었습니다.
  중복을 만들지 않고 테스트 하나(`repeatedTicksNeverLoadAnIconMoreThanOnce`)로 셋을 함께 메웠습니다.
  실제 `CPUCardView`·`MemoryCardView`를 `sizeThatFits`로 tick마다 렌더링하며,
  두 카드가 같은 앱 5개를 그려 한 tick에 행 조회 10번·로더 호출 5번이 일어나고 이어지는 9 tick의 추가 호출이 0입니다.
  아이콘을 얻는 앱 3개와 못 얻는 앱 2개를 섞어 부정 결과 캐시도 이 단언에 걸리게 했습니다.
  **캐시 수명 공백이 실제였습니다** — 뷰가 tick마다 자기 캐시를 만드는 mutation에서
  **340개 중 새 테스트 1건만** 깨끗하게 실패합니다(main이 직접 재현).
  task-009의 캐시 단독 테스트 7건은 전부 통과해, 이 자리에 그물이 0이었음이 실증됐습니다.
  mutation 넷 확인 — 부정 결과 캐시 제거(2건), 조회 앞 캐시 비우기(5건), 뷰의 tick 단위 캐시 생성(1건),
  정원 자르기 제거(task-001 단언 2건). production 코드는 한 글자도 고치지 않았습니다.
  **남은 그물 밖** — 상세 목록 20행·증가량 순위 행은 이 테스트가 덮지 못합니다.
  `CPUDetailView`·`MemoryDetailView`·`TopApplicationsView`·`ApplicationProcessGroupListView`가 `private`이라
  테스트에서 렌더할 수 없고 `.popover` 콘텐츠는 `sizeThatFits`에서 평가되지 않습니다.
  같은 캐시 인스턴스를 쓰므로 카드에서 채워진 키는 상세에서도 로더를 부르지 않지만 실행으로 확인되지는 않았습니다.
  접근 수준을 넓히는 것은 범위 밖이라 하지 않았습니다.
  `ApplicationCoordinator`가 캐시를 앱 수명 동안 하나만 드는 것도 그물 밖입니다 —
  `init`에서 인라인 생성하고 저장 속성으로 두지 않아 테스트가 관찰할 대상이 없습니다(구조적으로는 `init` 한 번뿐).
- 2026-08-22: **task-007 보류 해제 — 카드에 구성 합계 수치 하나를 둡니다(사용자 결정).**
  코드는 Per-Request로 구현했고 체크박스는 `[ ]`입니다(verify 전).
  **결정** — 항목별 네 수치는 카드에 두지 않고, 구성 합계 하나를 Pressure·Swap 병합 줄 맨 뒤에 「구성 <값>」으로 둡니다.
  제목 줄의 「사용 중」과 다른 줄·다른 라벨이라 `SPEC §5.3`의 「구성 합계와 「사용 중」이 서로 다른 지표로 구분되어 읽힘」이 카드에서 성립합니다.
  범례는 스와치+전체 이름 네 개(219pt)를 그대로 유지하고 제목 줄의 바도 95pt를 그대로 씁니다.
  **재실측으로 선택지를 좁혔습니다**(`NSHostingController.sizeThatFits`, 카드 콘텐츠 폭 232pt = 248 − padding 16).
  이전 세션 값(219 / 347 / 여유 96pt)이 그대로 재현됐고(219 / 345 / 95pt), 여기에 중간안을 더 재봤습니다 —
  스와치+이름 네 글자(App/Wired/Comp/Cach)+수치는 253~273pt, 세 글자(App/Wir/Cmp/Cch)+수치는 241~250pt로 모두 넘칩니다.
  들어가는 것은 이니셜+수치(210pt, 16GB 최댓값에서 233pt로 넘침)와 세 글자+정수 수치(217pt)뿐이라,
  **항목별 수치를 카드에 두려면 이름 축약과 소수 자리 포기 중 하나가 필수**입니다.
  구성 합계를 제목 줄에 두는 안도 재봤습니다 — 바가 95 → 32pt로 줄어(128GB 기기에서 22pt) 바를 사실상 죽이므로 접었습니다.
  병합 줄은 155pt만 쓰고 있어 여유 77pt에 합계가 들어갑니다.
  **구현 결과 폭 실측**(production `ByteCountFormatter(countStyle: .memory)` 출력 기준) —
  대표값 213pt, 16GB 기기 최댓값 228pt, 128GB·Swap 100GB 219pt로 모두 232pt 안에 들어갑니다.
  Swap 사용량과 10분 변화량이 **동시에** 두 자리 GB인 극단 입력(세 수치가 모두 35.9 GB급)에서만 233~243pt로 넘쳐 끝이 잘리며,
  이는 `ANALYSIS §5 DP4`가 병합 줄에 대해 이미 받아들인 대가입니다.
  **문서 상태** — `SPEC §5.3`은 고치지 않았습니다(수치를 두는 쪽이 원래 요구입니다).
  `ANALYSIS §5 DP4`의 채택안(옵션 D, 「네 항목의 이름과 수치가 카드에 남습니다」)은 이 결정으로 대체됐으나
  analyze.md 전문은 다시 쓰지 않았습니다 — 부분 수정하지 않는 문서라서이고, 재작성 여부는 사용자 판단으로 남깁니다.
  implement.md task-007의 목적·접근·검증 조건은 실제 구현에 맞춰 갱신하고 참조에 이 대체 사실을 적었습니다.
  **남은 그물 밖 (verify 전에 메워야 할 자리)** — `MemoryCompositionBytes.total`이 `app`만 돌려주게 바꾸는 mutation이
  340개를 **전부 통과합니다**(main이 직접 재현). 카드 표시 값을 검사하는 테스트가 없어 호출부가 「사용 중」을 넘기는 mutation도 같은 공백입니다.
  병합 줄 폭 초과(구성 합계 잘림)도 `lineLimit(1)` 때문에 렌더 높이 단언이 잡지 못합니다.
  단위 테스트 340개는 전부 통과하고 카드 높이 기준값 181.0pt 단언도 그대로 통과합니다.
- 2026-08-22: task-007 회귀 그물 보강. 단위 테스트 **345개 전부 통과**(340 → 345).
  앞 항목이 「verify 전에 메워야 할 자리」로 적은 공백 둘을 닫았습니다.
  `MemoryCompositionTotalTests` 넷 — 합계가 네 항목 전부의 합인지, 넘침으로 구간이 잘려도 합계는 실제 바이트인지,
  `UInt64`를 넘는 입력에서 trap 대신 상한에서 멈추는지, 「사용 중」만 다른 두 표시 값이 같은 합계를 내는지.
  `MemoryCardCompositionTotalRenderingTests` 하나 — **뷰가 실제로 그리는 값**을 관찰합니다.
  **관찰 수단을 찾는 데 두 번 실패했습니다** — 카드는 `.frame(maxWidth: .infinity)`라 무한 폭 제안에
  `sizeThatFits`가 `Double.greatestFiniteMagnitude`를 돌려주고, `.fixedSize(horizontal:)`로 유한값(235pt)을 얻어도
  카드에서 가장 넓은 줄이 병합 줄이 아니라 그 값이 구성 합계 변화에 반응하지 않습니다.
  Swap을 크게 잡아(999GB, 변화 기준점 포함) 병합 줄을 카드에서 가장 넓은 줄로 만든 뒤에야
  「구성 바이트만 다른 두 카드의 필요 너비가 다르다」가 성립했습니다.
  높이 단언으로는 이 자리를 볼 수 없습니다 — `lineLimit(1)`이 줄바꿈 대신 끝을 자르므로 줄 내용이 바뀌어도 높이가 같습니다.
  **mutation 셋을 실측 확인했습니다** — 뷰가 `usedBytes`를 넘기면 렌더 테스트 1건만 깨끗하게 실패하고,
  합계가 `app`만 돌려주면 3건(직전까지는 340개 전부 통과했던 자리), 병합 줄에서 합계 요소를 지우면 4건이 실패합니다.
  **남은 한계** — 병합 줄이 콘텐츠 폭을 넘겨 구성 합계가 잘리는 경우는 여전히 그물 밖입니다(`lineLimit(1)` 때문).
- 2026-08-22: **task-007 완료**(verify `approved`). 닫히는 `SPEC §5.N`은 없습니다 —
  §5.3은 task-008·011·012가, §5.8은 task-008·011이 남습니다.
  verifier가 단위 345개 통과와 mutation 3종(뷰가 `usedBytes`를 넘김 1건, 합계가 `app`만 반환 3건, 합계 요소 삭제 4건)을
  독립 실행으로 재현했고, 바 높이 8 → 30 mutation이 높이 단언 2건에서 잡히는 것도 함께 확인했습니다.
  **verifier가 main이 보고하지 않은 공백을 찾았습니다** — 제목 줄에서 `MemoryCompositionBarView` 호출을
  통째로 지워도 345개가 전부 통과했습니다. 바의 존재 자체에 그물이 0이었습니다.
  reject 사유로 세지 않은 이유는 task-007 `확인` 필드가 이 mutation을 요구하지 않고
  `ANALYSIS §5 DP15` 옵션 B가 「뷰 본문 안에서만 성립하는 실수는 남는다」를 이미 받아들였기 때문입니다.
  **그 공백을 닫았습니다** — `MemoryCardCompositionBarRenderingTests.cardDrawsCompositionSegments`.
  구성 합계는 같고 분포만 다른 두 표시 값(App 8GiB·Cached 0 / App 0·Cached 8GiB)을 `ImageRenderer`로 그려
  픽셀이 다른지 봅니다. 합계가 같아 병합 줄 문자열이 같으므로 두 그림이 갈리는 자리는 바의 구간뿐입니다.
  **크기 측정으로는 닿지 않는 자리였습니다** — 바 높이 8pt가 제목 텍스트 높이 안에 들어가 렌더 높이가 그대로이고,
  바 너비는 제목 줄의 남는 폭이라 카드 필요 너비에도 드러나지 않습니다.
  텍스트가 아닌 그림 요소를 단위 테스트로 잡는 첫 수단이라 task-011의 「값이 없을 때 구성 바를 그리지 않게 하면 실패해야」에도 쓸 수 있습니다.
  **mutation 둘을 실측 확인했습니다** — 바를 카드에서 통째로 지우면 이 테스트 1건만, 구간 그리기를 지워 트랙만 남겨도 1건만 실패합니다.
  단위 **346개 전부 통과**(345 → 346).
- 2026-08-29: 진행 추적자에 후속 항목 둘을 등록했습니다(코드 변경 없음).
  (1) task-012 확인 필드에 **`SPEC §5.3` 재판정**을 적었습니다 —
  「각 구간이 … 수치를 확인할 수 있습니다」에서 항목별 네 수치를 카드 밖으로 뺐으므로,
  접근성 이름과 상세 범례만으로 이 문장이 카드에서 성립하는지를 task-012 approve 시점에 판단해야 합니다.
  `docs/product.md` 125행의 「중요한 분석 정보는 Hover에만 의존하지 않습니다」와 나란히 볼 자리입니다.
  task-007 verify가 남긴 위험인데 어느 문서에도 없던 것을 옮겼습니다.
  (2) task-011 확인 필드에 **task-010의 행 식별자·탭 동작 실행 확인**을 적었습니다 —
  기기 인증 창이 풀린 뒤 `DashboardProcessListDisplayUITests`·`DashboardDetailExpansionUITests`로 닫습니다.
  2026-08-21 task-010 항목의 「남은 위험 ②」가 본문에만 있고 어느 Task에도 걸려 있지 않았습니다.
  **아직 열려 있는 것 — `ANALYSIS §5 DP4` 재작성 여부.** 채택안(옵션 D, 「네 항목의 이름과 수치가 카드에 남습니다」)이
  실제 구현과 다른 채로 남아 있고, 대체 사실은 이 README 2026-08-22 첫 항목과 implement.md task-007 참조 필드에만 있습니다.
  analyze.md는 부분 수정하지 않는 문서라 `/analyze-init` 재작성이 필요하고, 진행 여부는 사용자 판단입니다.
- 2026-08-29: **task-008 완료**(verify `approved`). **`SPEC §5.4`가 닫혔습니다** —
  §5.3은 task-011·012가, §5.8은 task-011이 남습니다.
  구현은 Orca orchestration으로 codex worker가, 판정은 별도 claude worker가 독립 수행했습니다.
  `MemoryDetailView`의 구성 텍스트 한 줄이 도넛 + 수치 범례 + 별도 「사용 중」 줄로 바뀌었고,
  뷰 밖에 순수 타입 셋이 생겼습니다 — `MemoryCompositionDonutLayout`(각도 변환),
  `MemoryCompositionDetailLegendFormatting`(범례 행 서식), `MemoryCompositionDetailSummary`(구성 합계 / 「사용 중」 분리).
  **도넛이 새 분모를 두지 않습니다** — 카드의 `compositionLayout` 결과를 받아 `-90 + 360 × startRatio`로 각도만 옮기므로
  카드와 상세가 갈릴 경로가 구조에 없고, 넘침 자르기도 카드 계산이 이미 한 값을 그대로 씁니다.
  새 테스트 6개(도넛 3·범례 2·요약 1)를 `MemoryCompositionDetailTests.swift`에 뒀습니다.
  **mutation 7종이 전부 잡힙니다**(verifier가 직접 넣고 재현 후 원복, `shasum` 대조 확인) —
  네 항목 합 정규화 3건, 시작 방향 12시→3시 2건, 회전 방향 반전 2건, 넘침 자르기 제거 3건,
  가운데 수치를 「사용 중」으로 1건, 범례 수치 제거 2건, 카드 축약 서식 바꿔치기 1건, 상세 전용 계산 분리 1건.
  단위 스위트는 실패 0입니다(`TEST SUCCEEDED`).
  **테스트 개수 세는 방식이 갈립니다** — `Test case … passed` 줄 수는 388, 서로 다른 테스트 이름은 351개입니다.
  앞선 기록의 346은 또 다른 방식으로 센 값으로 보이며, 실패 건수는 어느 방식으로도 0입니다.
  **남은 한계 넷** —
  ① `MemoryDetailView` 뷰 본문에 회귀 그물이 **0**입니다. 축약 서식 주입·가운데 값 교체·범례 수치 제거를 한꺼번에 넣어도,
  나아가 `MemoryCompositionDonutView` 호출을 **통째로 지워도** 단위 스위트가 전부 통과합니다.
  `MemoryDetailView`·`MemoryCompositionDonutView`가 `private`이고 `.popover` 콘텐츠가 `sizeThatFits`에서 평가되지 않아서입니다
  (2026-08-21 task-013 항목이 적은 것과 같은 벽). task-007에서 쓴 `ImageRenderer` 수단은 `MemoryCardView`가
  기본 접근 수준이라 가능했던 것이고 상세에는 그대로 적용되지 않습니다.
  task-008 `확인` 필드가 이 mutation들을 요구하지 않고 `ANALYSIS §5 DP15` 옵션 B가 이 잔여 범위를 이미 받아들여 반려 사유로 세지 않았습니다.
  ② 팝업 400×480 고정은 **실행으로 확인하지 못했습니다** — UI 스위트가 기기 인증 창으로 막혀 있어
  `MemoryDetailPopoverContent`의 `ScrollView`·`.frame`이 diff에 없다는 정적 근거뿐입니다.
  140pt 도넛이 480pt 안에서 스크롤 양을 얼마나 늘리는지는 재지 않았습니다.
  ③ 범례의 `-` 분기는 production에서 도달하지 않습니다(`compositionBytes`가 비옵셔널이고 `.normal`에서만 그려짐).
  검증 조건이 명시한 동작이라 문제로 세지 않았습니다.
  ④ 「카드보다 큰 형태」는 소스 상수 대조(도넛 지름 140pt·링 18pt 대 카드 바 높이 8pt)로만 판단했고 렌더 크기를 재지 않았습니다.
