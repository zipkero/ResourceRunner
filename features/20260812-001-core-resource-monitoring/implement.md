# CPU·Memory 핵심 리소스 모니터링 구현

## 체크리스트

## Section: 시스템 지표 수집과 일정

- [x] task-001: CPU·Memory 시스템 지표 한 tick 수집
  - 목적: 한 번의 수집으로 전체 CPU 사용률, User·System·Idle 비율, 논리 코어별 사용률, Load Average,
    전체 물리 메모리, 사용 중 메모리, App·Wired·Compressed·Cached, Swap 사용량, Memory Pressure 단계가
    같은 시각에 묶인 결과 하나로 나오고,
    CPU와 Memory 중 한쪽 조회가 실패해도 다른 쪽 값이 그 시각의 결과에 그대로 남습니다.
  - 접근: CPU Collector가 `host_processor_info(PROCESSOR_CPU_LOAD_INFO)`로 논리 코어별 tick을 읽어
    직전 tick 원본과의 차이로 사용률을 만들고 Load Average는 `getloadavg`로 읽습니다.
    Memory Collector가 `host_statistics64(HOST_VM_INFO64)`, `host_page_size`, `ProcessInfo.physicalMemory`,
    `sysctl(VM_SWAPUSAGE)`, `sysctlbyname("kern.memorystatus_vm_pressure_level")`을 한 tick에 함께 읽습니다.
    두 Collector를 소유한 actor source가 CPU와 Memory 각각을 `Result`로 담은 샘플 하나를 반환하고,
    지표별 실패는 던지지 않고 값으로 전달합니다.
    직전 tick 원본이 없거나 직전 tick과의 간격이 예상 주기의 허용 배수를 넘으면 사용률을 만들지 않고 기준점만 갱신합니다.
  - 검증 조건:
    - 결과: 코어별 사용률 개수가 `ProcessInfo.activeProcessorCount`와 같고 각 값이 0~100% 범위이며,
      전체 사용률은 코어별 tick 합에서 계산되어 항상 0~100% 범위이고 User·System·Idle의 합이 100%에 수렴합니다.
      첫 tick은 기준점만 잡고 CPU 사용률을 만들지 않으며 두 번째 tick부터 값이 나옵니다.
      직전 tick과의 간격이 허용 배수를 넘으면 값 없이 기준점만 갱신됩니다.
      Memory는 순간값 조회이므로 첫 tick부터 값이 나오고, 사용 중 메모리가 전체 물리 메모리를 넘지 않습니다.
      Memory Pressure 원시값 `0x01`·`0x02`·`0x04`만 `normal`·`warning`·`critical`로 해석되고 그 밖의 값은 실패입니다.
      CPU 조회가 실패한 tick의 샘플에도 Memory 성공값이 그대로 담기고 그 반대도 같으며,
      실패한 지표가 0이나 빈 값으로 바뀌지 않습니다.
      두 지표가 하나의 샘플 시각을 공유해 카드 사이에 시점 차이가 생기지 않습니다.
    - 확인: Swift Testing으로 tick 원본과 시각을 주입한 차분 계산을 검증합니다 —
      첫 tick의 값 부재, 정상 간격에서의 사용률 산출, 허용 배수 초과 간격에서의 기준점 전용 갱신,
      코어별 값과 전체 값의 관계, User·System·Idle 합을 각각 단언합니다.
      Memory Pressure 원시값 매핑은 세 유효값과 무효값 표본으로 전수 검증합니다.
      실패 격리는 실패를 반환하는 가짜 Collector를 주입해 확인하며,
      이 테스트가 고정하는 것은 "한 지표의 실패가 같은 tick의 다른 지표 값을 함께 없애지 않는다"입니다 —
      source가 실패를 던지도록 되돌리면 이 테스트가 실패해야 합니다.
      실제 시스템 호출 경로는 macOS 26.5 Apple silicon에서 연속 두 tick을 수집해
      코어 수 일치와 값 범위를 단언하는 테스트로 확인합니다.
  - 참조: SPEC §5.1, SPEC §5.10, ANALYSIS §1 「수집 경계」, ANALYSIS §2 「시스템 지표 tick」,
    ANALYSIS §3 「수집 계약」, ANALYSIS §5 DP2, ANALYSIS §5 DP9, ANALYSIS §5 DP11

- [x] task-002: 주기 변경에서 살아남는 최근 10분 이력
  - 목적: 팝오버 열림·닫힘이나 저전력 모드로 수집 주기가 바뀌어도 이미 쌓인 최근 10분 이력이 사라지지 않고,
    사용률을 만들지 못한 tick은 이력에 남지 않으며,
    표시로 넘어가는 구간이 최신 샘플 시각 기준 10분 창으로 한정됩니다.
  - 접근: 샘플 저장소를 최신 스냅샷 하나와 시각·전체 CPU 사용률·Swap 사용 바이트만 담는 이력 링으로 나누고,
    링 용량을 이 feature가 쓸 수 있는 가장 짧은 주기 기준으로 고정해 resize 경로를 제거합니다.
    저장소는 `MonitoringSampleSink` 계약을 구현하고 `MonitoringScheduler`는 저장 대상을 구체 타입 대신 이 계약으로 받습니다.
    표시용 값을 내보낼 때 최신 샘플 시각에서 10분을 뺀 시점 이후의 항목만 선별합니다.
    저장소가 `SystemMetricsSample` 전용이 되면서 자리표시 source와 타입이 맞지 않으므로
    코디네이터 배선을 실제 시스템 지표 source로 바꾸고, `MonitoringSampleSink` 제네릭 파라미터는
    Scheduler를 보유하는 타입까지 전파됩니다. 자리표시 심볼 제거는 task-014에 남깁니다.
  - 검증 조건:
    - 결과: 수집 주기가 1초에서 2초, 5초로 바뀌어도 링 용량이 그대로이고 이미 담긴 항목의 값과 순서가 변하지 않습니다.
      CPU 사용률과 Swap 값이 모두 있는 tick만 링에 추가되고, 사용률을 만들지 못한 tick은 링에 들어가지 않아
      그 시각이 이력에서 비어 있게 됩니다.
      최신 스냅샷은 매 tick 교체되어 코어별 사용률과 Memory 세부 구성처럼 현재값만 필요한 지표를 담습니다.
      중지 구간을 사이에 둬서 10분보다 오래된 항목이 링에 남아 있어도 표시용 값에는 포함되지 않습니다.
      저장소 밖으로 나가는 경로는 표시용 값 stream과 snapshot뿐이고 가변 버퍼 참조는 노출되지 않으며,
      stream은 최신 조합 하나만 보존해 소비가 밀려도 오래된 조합이 쌓이지 않습니다.
      주기 기반 `resize` API는 남지 않습니다.
    - 확인: Swift Testing으로 시각을 직접 준 샘플 시퀀스를 사용합니다.
      이 테스트가 고정하는 것은 M1의 resize 회귀입니다 —
      1초 주기로 이력을 채운 뒤 주기를 2초·5초로 바꾸고 저장된 값 배열 전체를 비교해
      한 항목도 사라지지 않았음을 단언하며, 용량을 유효 주기로 재계산하도록 되돌리면 이 테스트가 실패해야 합니다.
      값이 없는 tick의 링 미추가, 10분 창 선별(창 경계 직전·직후 항목), 링 용량 초과 시 가장 오래된 항목만 교체,
      1시간 중지를 시각으로 재현한 뒤의 표시용 값이 빈 목록이 되는 것을 각각 단언합니다.
      가변 참조 미노출과 `resize` 부재는 API 시그니처와 소스 검색으로 확인합니다.
  - 참조: SPEC §5.1, SPEC §5.11, ANALYSIS §1 「이력 경계」, ANALYSIS §2 「시스템 지표 tick」,
    ANALYSIS §4 「기존 코드」, ANALYSIS §5 DP1, ANALYSIS §5 DP3

- [x] task-003: 두 수집 축의 일정과 화면을 볼 수 없는 상태
  - 목적: 팝오버 열림·닫힘과 저전력 모드에 따라 시스템 지표와 프로세스 조사의 주기가 각각 정해진 값으로 바뀌고,
    화면 잠금·디스플레이 슬립·빠른 사용자 전환 중 하나라도 성립하면 두 수집이 모두 멈춰 새 샘플이 쌓이지 않습니다.
  - 접근: `SystemLifecycleSnapshot`과 `SystemLifecycleFieldChange`에 디스플레이 슬립과 세션 활성 필드를 더하고,
    `NSWorkspaceScreensDidSleep`·`ScreensDidWake`·`SessionDidResignActive`·`SessionDidBecomeActive` 알림을 등록하며
    초기값은 화면이 켜져 있고 세션이 활성인 상태로 둡니다.
    `CollectionSchedulePolicy`가 두 일정을 묶은 `CollectionSchedulePlan`을 계산하고,
    `MonitoringLifecycleStore`가 `CollectionScheduleTarget` 계약으로 두 Scheduler를 보유해
    각 축의 결과가 바뀔 때만 그 축에 적용합니다.
  - 검증 조건:
    - 결과: normal 열림은 시스템 1초·프로세스 2초, normal 닫힘은 2초·5초,
      lowPower 열림은 2초·4초, lowPower 닫힘은 5초·10초입니다.
      화면 잠금 `locked`, 잠금 `unknown`, 디스플레이 슬립, 세션 비활성 각각이 단독으로 두 일정을 모두 `paused`로 만들고,
      중지 동안 두 축의 누적 샘플 수가 늘지 않으며 재개 시 놓친 실행을 따라잡지 않습니다.
      디스플레이 슬립과 세션 활성의 초기값은 알림이 오지 않아도 앱이 수집을 시작할 수 있는 값이고,
      이후 알림으로 교정됩니다.
      같은 결과를 만드는 이벤트가 반복 도착해도 해당 축의 `apply(_:)`가 다시 호출되지 않고 실행 중인 작업이 재시작되지 않습니다.
      한 축의 일정만 바뀌면 다른 축의 실행 중 작업은 취소되지 않습니다.
      M1의 revision 거부, 단일 Task·generation, 기준 deadline 전진 규칙은 축마다 그대로 유지됩니다.
    - 확인: 메모리 생명주기 source와 수동 clock, 주입 sample source로 Swift Testing 검증을 작성합니다.
      팝오버 2 × 전력 2 × 잠금 3 × 디스플레이 슬립 2 × 세션 활성 2 조합에서 `CollectionSchedulePlan`의 두 값을 전수 단언합니다.
      이 테스트가 고정하는 것은 두 가지입니다 —
      "새로 더한 두 신호 각각이 단독으로 수집을 멈춘다"(두 필드 중 하나를 판정에서 빼면 실패),
      "한 축의 일정 변경이 다른 축을 재시작하지 않는다"(축별 `apply(_:)` 호출 횟수와 시계 전진 후 누적 샘플 수로 관찰).
      중지 상태에서 시계를 전진시켜 두 축의 샘플 수가 정확히 0만큼 늘어나는지 세고,
      재개 후 샘플 수가 경과 시간이 아니라 tick 수만큼만 늘어나는지 확인합니다.
      실제 조작에서의 중지·재개는 task-012에서 관찰합니다.
  - 참조: SPEC §5.12, ANALYSIS §1 「생명주기 경계」, ANALYSIS §2 「수집 중지와 재개」,
    ANALYSIS §3 「일정 계약」, ANALYSIS §5 DP1, ANALYSIS §5 DP12

## Section: 프로세스 조사와 앱 집계

- [x] task-004: 사용자 소유 프로세스 조사
  - 목적: 한 번의 조사로 현재 사용자 소유 프로세스마다 PID, 시작 시각, 실행 경로, 부모 PID,
    누적 CPU 시간, Resident Memory와 Apple silicon 네이티브·Rosetta 실행 여부가 나오고,
    읽지 못한 프로세스는 개수로만 세어질 뿐 사용량이 추정값으로 채워지지 않습니다.
  - 접근: `sysctl(KERN_PROC_ALL)`로 `kinfo_proc` 배열을 얻어 PID, `p_starttime`, uid, 부모 PID,
    `P_TRANSLATED` 플래그를 함께 읽습니다.
    현재 유효 uid와 같은 프로세스에만 `proc_pidinfo(PROC_PIDTASKINFO)`를 호출해 누적 CPU 시간과 Resident Memory를 읽고,
    uid가 다르거나 호출이 실패한 프로세스는 결과에서 빼고 읽지 못한 수에 더합니다.
    실행 경로는 새로 관찰된 정체성에 대해서만 `proc_pidpath`로 한 번 읽습니다.
  - 검증 조건:
    - 결과: 결과 목록의 모든 항목이 현재 유효 uid 소유이고, root 소유 프로세스는 목록에 없으며 읽지 못한 수에 포함됩니다.
      읽지 못한 수는 실기기 조사에서 0보다 큽니다.
      자기 프로세스가 목록에 있고 Resident Memory와 누적 CPU 시간이 0보다 큽니다.
      Rosetta 실행 여부가 `P_TRANSLATED` 플래그에서 그대로 나오고 추가 시스템 호출 없이 채워집니다.
      uid가 다른 프로세스에는 `proc_pidinfo`가 한 번도 호출되지 않습니다.
      이미 경로를 읽은 정체성에는 `proc_pidpath`가 다시 호출되지 않습니다.
      실패한 프로세스의 CPU 시간과 메모리가 0이나 다른 값으로 채워지지 않습니다.
    - 확인: macOS 26.5 Apple silicon에서 실제 조사 1회를 수행하는 Swift Testing 검증으로 위 단언을 확인합니다.
      이 테스트가 고정하는 것은 "읽지 못한 프로세스를 값으로 채우지 않는다"와 "uid 사전 판별을 건너뛰지 않는다"입니다 —
      호출 카운터를 주입해 uid가 다른 프로세스 수만큼의 `proc_pidinfo` 호출이 발생하면 실패하도록 단언하고,
      결과 목록 크기와 읽지 못한 수의 합이 열거된 전체 프로세스 수와 같은지 확인합니다.
      경로 캐시는 같은 정체성을 두 번 조사해 `proc_pidpath` 호출 횟수가 1인지로 확인합니다.
  - 참조: SPEC §5.6, ANALYSIS §1 「수집 경계」, ANALYSIS §2 「프로세스 조사 tick」,
    ANALYSIS §3 「프로세스와 앱 계약」, ANALYSIS §5 DP7

- [x] task-005: 정체성별 프로세스 이력과 사라진 프로세스 제거
  - 목적: 프로세스가 종료되거나 접근에 실패하거나 PID가 재사용되어도
    사라진 프로세스가 순위 입력에 남거나 다른 프로세스가 이전 이력을 이어받는 일이 없고,
    프로세스 CPU 사용률이 논리 코어를 합산한 값으로 계산되어 여러 코어를 쓰는 프로세스에서 100%를 넘습니다.
  - 접근: PID와 프로세스 시작 시각의 쌍을 정체성 키로 삼는 사전을 두고,
    정체성마다 직전 누적 CPU 시간과 그 시각, 순위 안정화용 최근 세 개 값 링,
    최소 간격을 둔 메모리 기준점 링을 보관합니다.
    매 조사에서 관찰되지 않은 정체성은 그 자리에서 사전에서 제거하고,
    직전 시각과의 간격이 허용 범위를 넘으면 사용률을 만들지 않고 기준점만 갱신합니다.
  - 검증 조건:
    - 결과: 같은 PID가 다른 시작 시각으로 나타나면 새 정체성이 되어 이전 누적 CPU 시간과 차분되지 않고
      첫 조사에서 사용률이 만들어지지 않으며 메모리 기준점도 새로 시작합니다.
      종료된 프로세스는 다음 조사에서 사전에서 제거되어 이후 순위 입력에 나타나지 않습니다.
      접근 실패로 조사 결과에서 빠진 프로세스도 제거되고, 다시 나타나면 새 기준점부터 시작합니다.
      누적 CPU 시간 차이를 경과 시간으로 나눈 값이 코어 합산 비율이라
      두 코어를 완전히 쓰는 프로세스가 200%로 계산됩니다.
      메모리 기준점 링의 크기는 조사 주기가 2초든 10초든 같고, 최소 간격보다 촘촘한 조사에서는 기준점이 추가되지 않습니다.
      중지 구간을 사이에 둔 두 조사에서는 사용률이 만들어지지 않고 기준점만 갱신됩니다.
    - 확인: 합성 조사 시퀀스를 입력으로 주는 Swift Testing 검증을 작성합니다.
      이 테스트가 고정하는 것은 "정체성 키에서 시작 시각을 빼면 PID 재사용에서 이력이 섞인다"입니다 —
      같은 PID·다른 시작 시각·큰 누적 CPU 시간 차이를 주는 시퀀스에서 사용률이 만들어지지 않아야 하고,
      키를 PID만으로 되돌리면 이 테스트가 실패해야 합니다.
      종료 정체성의 즉시 제거는 조사 두 번 뒤 사전 크기와 키 집합으로 단언하고,
      코어 합산 계산은 경과 시간의 두 배에 해당하는 누적 CPU 시간 증가에서 200%가 나오는지로 단언합니다.
      기준점 링의 주기 독립성은 2초·10초 두 시퀀스에서 같은 링 크기가 나오는지로 확인합니다.
  - 참조: SPEC §5.3, SPEC §5.7, ANALYSIS §1 「이력 경계」, ANALYSIS §2 「프로세스 조사 tick」,
    ANALYSIS §2 「앱 집계와 순위」, ANALYSIS §3 「프로세스와 앱 계약」, ANALYSIS §5 DP4, ANALYSIS §5 DP6

- [x] task-006: 앱 단위 TOP 5와 최근 증가량 순위
  - 목적: 여러 프로세스로 구성된 앱이 하나의 항목으로 묶여 TOP 5에 나타나고,
    순위가 순간값이 아니라 최근 여러 샘플을 반영해 매 갱신마다 요동치지 않으며,
    현재 메모리를 많이 쓰는 앱과 최근 10분 사이 메모리가 많이 늘어난 앱이 서로 다른 목록으로 제공됩니다.
  - 접근: 실행 경로에서 가장 바깥 `.app` 번들 경로를 앱 키로 삼고, `.app`를 찾지 못하면 실행 파일 경로를 키로 씁니다.
    표시 이름은 번들 이름이거나 실행 파일 이름이고, 경로에서 키와 이름을 유도한 결과는 고정 상한 LRU로 캐시합니다.
    현재값 순위는 정체성별 최근 세 개 값의 평균을 앱 키로 합산해 만들고,
    증가량 순위는 정체성별 메모리 기준점 링에서 10분 창 안의 가장 오래된 기준점과 현재값의 차이를 앱 키로 합산해 만듭니다.
    CPU와 Memory가 같은 앱 키 규칙을 공유하고, 읽지 못한 프로세스 수를 순위 결과와 함께 전달합니다.
  - 검증 조건:
    - 결과: 상위 앱 번들 안에 중첩된 helper 번들 경로가 가장 바깥 `.app` 키로 접혀 한 항목으로 합산되고,
      `.app`가 없는 명령행 실행 파일 경로는 그 경로 자체가 키가 됩니다.
      한 tick만 급등한 앱이 최근 세 개 평균 때문에 1위가 되지 않고, 연속 갱신에서 상위 5개의 순서가 뒤집히지 않습니다.
      현재 사용량 순위와 증가량 순위가 서로 다른 목록으로 나오며,
      사용량이 크지만 변화가 없는 앱이 증가량 순위 상위에 오르지 않고 그 반대도 같습니다.
      10분 창 밖의 기준점은 증가량 계산에 쓰이지 않습니다.
      CPU 순위와 메모리 순위가 같은 앱 키 집합 위에서 계산됩니다.
      읽지 못한 프로세스 수가 순위 결과에 함께 담겨 표시 계층으로 전달됩니다.
    - 확인: 경로 표본으로 앱 키 유도를 전수 검증합니다 —
      중첩 helper 번들, `.app` 안의 `.app`, 번들 밖 명령행 바이너리, 심볼릭 링크 형태 경로를 각각 단언합니다.
      순위 계산은 합성 이력으로 검증하며,
      이 테스트가 고정하는 것은 "순간값으로 순위를 만들지 않는다"입니다 —
      직전 두 조사에서 낮았고 이번 조사에서만 급등한 앱이 1위가 되면 실패하도록 단언하고,
      평균을 최신값으로 되돌리면 이 테스트가 실패해야 합니다.
      증가량 순위는 현재값이 큰 앱과 증가량이 큰 앱을 다르게 구성한 입력에서 두 목록의 1위가 다름을 단언합니다.
      가장 바깥 `.app` 규칙이 실제 앱에서 성립하는지는 근거가 추정이므로,
      macOS 26.5 Apple silicon에서 Chrome이나 Electron 기반 앱과 IDE를 띄운 채 조사 결과를 확인해
      하위 프로세스가 상위 앱 하나로 묶이는지 직접 관찰하고, 묶이지 않는 앱이 있으면 그 사실을 기록합니다.
  - 참조: SPEC §5.6, SPEC §5.8, ANALYSIS §1 「계산 경계」, ANALYSIS §2 「앱 집계와 순위」,
    ANALYSIS §3 「프로세스와 앱 계약」, ANALYSIS §5 DP5, ANALYSIS §5 DP6

## Section: 메뉴바와 대시보드 표시

- [x] task-007: 실제 CPU 부하를 따르는 메뉴바 표시 상태
  - 목적: 실제 CPU 부하가 오르내리면 메뉴바 항목의 접근성 이름이 낮음·보통·높음·매우 높음·장시간 고부하를 따라 바뀌고,
    값이 상태 경계 근처에서 흔들려도 표시 상태가 반복해서 뒤집히지 않으며,
    높은 부하가 오래 이어질 때만 장시간 고부하로 구분됩니다.
  - 접근: 전체 CPU 사용률, 샘플 시각, 직전 판정 상태를 받아 다음 상태를 돌려주는 순수 판정 함수를 두고,
    경계 25·50·75%, 하강 방향 5%p 데드밴드, 새 후보 상태의 최소 유지 시간 3초,
    75% 이상 60초 지속에서의 장시간 고부하 진입을 적용합니다.
    지속 시간은 샘플 개수가 아니라 샘플 시각으로 세고, 인접 샘플 간격이 허용 범위를 넘으면 누적을 끊습니다.
    코디네이터가 시스템 지표 소비 지점에서 판정하고 상태가 바뀌었을 때만 `CharacterStateSource.send(_:)`를 호출하며,
    CPU 지표가 실패한 tick에서는 판정을 진행하지 않고 마지막 상태를 유지합니다.
  - 검증 조건:
    - 결과: 상승은 경계값을 그대로 써서 25%에서 보통, 50%에서 높음, 75%에서 매우 높음 후보가 되고,
      하강은 각 경계에서 5%p를 뺀 값을 밑돌아야 낮은 상태로 내려갑니다.
      새 후보 상태는 3초 이상 유지될 때만 실제 표시 상태를 바꿉니다.
      경계를 사이에 두고 오르내리는 값(24%와 26% 교대)에서 표시 상태 변경이 한 번도 일어나지 않고,
      한 tick짜리 스파이크가 표시 상태를 바꾸지 않습니다.
      75% 이상이 60초 이상 끊기지 않고 이어지면 장시간 고부하가 되고 59초 시점에는 아직 매우 높음입니다.
      장시간 고부하에서는 사용률이 70% 아래로 3초 이상 내려가야 벗어납니다.
      수집이 중지된 구간을 사이에 둔 두 샘플은 하나의 지속으로 이어지지 않아 잠금 구간이 장시간 고부하로 판정되지 않습니다.
      CPU 수집이 실패한 tick에서 표시 상태가 바뀌지 않습니다.
      메뉴바 표시 경로는 M1과 같고 접근성 이름 한 자리만 갱신되며, 상태 입력이 주입값에서 실제 판정으로 바뀝니다.
    - 확인: 사용률과 시각을 직접 준 시퀀스로 순수 판정 함수를 Swift Testing 검증합니다.
      이 테스트들이 고정하는 것은 세 가지이며 각 장치를 제거하면 대응하는 테스트가 실패해야 합니다 —
      데드밴드를 없애면 24%·26% 교대 시퀀스에서 표시 상태 변경 횟수 0 단언이 실패하고,
      최소 유지 시간을 없애면 한 tick 스파이크 시퀀스가 실패하며,
      지속을 시각 대신 샘플 개수로 세면 5초 주기 시퀀스의 60초 판정과 1초 주기 시퀀스의 판정이 어긋나 실패합니다.
      경계 진입·이탈, 장시간 고부하 진입과 이탈, 중지 구간을 낀 지속 단절,
      실패 tick에서의 상태 유지를 각각 단언합니다.
      실기기 확인은 macOS 26.5 Apple silicon에서 앱을 실행한 채
      논리 코어 수만큼 `yes > /dev/null`을 띄워 부하를 올렸다 내리며 수행합니다 —
      `#if DEBUG` 로그(`.notice` 수준, `privacy: .public`)로 사용률과 판정 전이를 기록하고,
      접근성 검사기로 메뉴바 항목의 접근성 이름이 그 전이를 따라 바뀌는지,
      Debug 주입이 아니라 실제 판정이 이름을 정하는지 확인합니다.
      60초 이상 부하를 유지해 장시간 고부하 전이를 관찰합니다.
  - 참조: SPEC §5.4, ANALYSIS §1 「계산 경계」, ANALYSIS §1 「표시 경계」,
    ANALYSIS §2 「메뉴바 표시 상태 판정」, ANALYSIS §3 「표시 계약」, ANALYSIS §5 DP8

- [x] task-008: CPU 카드와 최근 10분 그래프
  - 목적: 팝오버를 열면 빈 화면이나 로딩 상태를 거치지 않고 마지막으로 수집된 값이 첫 프레임부터 보이며,
    CPU 카드에 전체 CPU 사용률, User와 System 비율, 최근 10분 그래프와 CPU 사용량 TOP 5가 표시되고,
    수집하지 않은 구간은 그래프에서 시각에 맞는 자리에 빈 채로 남습니다.
  - 접근: `@MainActor` 관찰 가능 표시 저장소가 팝오버가 닫혀 있어도 갱신을 계속 받아 최신 카드 표시 상태를 보유하고,
    팝오버 콘텐츠 뷰는 M1처럼 앱 시작 때 한 번 만들어져 계속 살아 있습니다.
    최신 스냅샷·이력 링·앱 단위 순위를 CPU 카드 표시 값으로 조립하고,
    그래프는 시각과 값을 묶은 점 목록을 시각 좌표로 그리며 인접 점의 간격이 예상 주기를 넘으면 선을 잇지 않습니다.
    가로축 오른쪽 끝은 그리는 시점의 시각으로 잡고, TOP 5에는 시스템 프로세스가 포함되지 않는다는 상시 문구를 함께 둡니다.
  - 검증 조건:
    - 결과: 앱 시작 직후 아직 유효한 값이 없으면 카드가 수집 중을 나타내고 첫 샘플부터 값을 표시합니다.
      팝오버를 여는 순간 표시 저장소가 이미 값을 들고 있어 로딩 표시가 나타나지 않고,
      이후 갱신으로 최신 값이 반영됩니다.
      그래프에는 실제로 수집된 구간만 나타나고 앱 시작 이전 구간이 채워지지 않습니다.
      인접 점의 간격이 예상 주기를 넘으면 두 점이 하나의 연결 구간에 들어가지 않아 그 사이가 빈 자리로 남습니다.
      가로축 오른쪽 끝이 그리는 시점의 시각이라 마지막 샘플이 오래된 상황에서도 빈 구간이 제자리에 보입니다.
      TOP 5는 앱 이름과 값을 최대 5개까지 표시하고 5개 미만이면 있는 만큼만 표시하며,
      시스템 프로세스가 포함되지 않는다는 문구가 Hover나 색상이 아니라 항상 보이는 자리에 있습니다.
      프로세스 CPU 값은 코어 합산 단위라 100%를 넘을 수 있고 그대로 표시됩니다.
      카드의 접근성 이름에 현재 사용률과 상태, TOP 5의 안내 문구가 포함됩니다.
    - 확인: 수집 결과에서 CPU 카드 표시 값을 만드는 조립 함수를 Swift Testing으로 검증합니다 —
      빈 이력, 중지 구간을 포함한 이력, 10분을 넘는 이력, TOP 5가 5개 미만·초과인 입력을 각각 단언합니다.
      이 테스트가 고정하는 것은 "빈 구간을 직선으로 잇지 않는다"입니다 —
      간격이 벌어진 점 쌍이 하나의 연결 구간에 포함되면 실패하도록 연결 구간 목록을 단언하고,
      간격 판정을 제거하면 이 테스트가 실패해야 합니다.
      팝오버를 여는 순간의 값 표시와 접근성 이름은 XCUITest로 확인합니다 —
      메뉴바 항목을 클릭해 팝오버를 열고 첫 조회에서 CPU 값 텍스트와 안내 문구가 존재하며
      로딩 문구가 나타나지 않는 것을 단언합니다.
      실제 조작에서의 빈 구간 관찰은 task-012에서 수행합니다.
  - 참조: SPEC §5.1, SPEC §5.3, SPEC §5.6, SPEC §5.9, SPEC §5.11, SPEC §5.13,
    ANALYSIS §1 「표시 경계」, ANALYSIS §2 「시스템 지표 tick」, ANALYSIS §3 「표시 계약」,
    ANALYSIS §5 DP3, ANALYSIS §5 DP10

- [x] task-009: Memory 카드와 색상에 기대지 않는 Memory Pressure 표시
  - 목적: Memory 카드에 전체 물리 메모리, 현재 사용 중 메모리, Memory Pressure 단계,
    Swap 사용량과 최근 변화량, 메모리 사용량 TOP 5가 표시되고,
    Pressure가 경고나 위험으로 바뀌면 색상 외의 수단으로도 그 단계가 드러나며 정상으로 돌아오면 원래 표시로 복귀합니다.
  - 접근: 최신 스냅샷과 앱 단위 메모리 순위를 Memory 카드 표시 값으로 조립하고,
    Swap 최근 변화량은 이력 링의 10분 창 안 가장 오래된 값과 현재값의 차이로 만듭니다.
    Pressure 단계는 텍스트 라벨과 형태가 구분되는 기호를 함께 써서 표시하고 카드 접근성 이름에 현재 단계를 포함하며,
    정상 단계에서는 같은 자리가 원래 표시로 돌아옵니다.
  - 검증 조건:
    - 결과: 세 단계가 서로 다른 텍스트 라벨과 서로 다른 형태의 기호를 가져 색을 제거해도 구분됩니다.
      경고나 위험에서 정상으로 돌아오면 라벨과 기호가 정상 표시로 복귀하고 다른 잔여 표시가 남지 않습니다.
      카드 접근성 이름에 현재 단계와 사용 중 메모리가 포함됩니다.
      Swap 최근 변화량은 10분 창 밖의 값을 기준으로 삼지 않고, 창 안에 값이 하나뿐이면 변화량을 만들지 않습니다.
      해석할 수 없는 Pressure 원시값은 실패로 다뤄져 카드가 그 사실을 나타내고 임의 단계로 표시되지 않습니다.
      메모리 TOP 5는 Resident Memory 기준이며 앱 단위로 묶여 표시됩니다.
    - 확인: 단계에서 표시 값을 만드는 매핑을 Swift Testing으로 전수 검증합니다.
      이 테스트가 고정하는 것은 "색상만으로 단계를 구분하지 않는다"입니다 —
      단언 대상에서 색상 속성을 빼고 라벨과 기호 식별자만 비교해 세 단계가 모두 다름을 확인하며,
      기호나 라벨을 한 값으로 통일하면 이 테스트가 실패해야 합니다.
      정상 복귀는 위험 → 정상 순서의 입력에서 표시 값이 최초 정상 표시와 같아지는지로 단언합니다.
      Swap 변화량은 10분 창 경계 직전·직후 기준점과 값이 하나뿐인 입력으로 검증합니다.
      실제 Memory Pressure 경고·위험은 임의로 유발하기 어려우므로 단계 값을 주입해 전환과 복귀를 확인하고,
      접근성 이름 노출은 XCUITest로 팝오버를 열어 Memory 카드 접근성 이름에 단계 문자열이 있는지 단언합니다.
  - 참조: SPEC §5.1, SPEC §5.5, SPEC §5.6, SPEC §5.13,
    ANALYSIS §1 「표시 경계」, ANALYSIS §2 「팝오버 열림과 카드 선택」, ANALYSIS §3 「표시 계약」,
    ANALYSIS §5 DP9, ANALYSIS §5 DP10

- [x] task-010: 카드 선택과 고정 상세 영역
  - 목적: 카드를 선택하면 팝오버 크기가 변하지 않은 채 고정된 하단 영역이 그 카드의 상세 지표로 채워지고
    다시 선택하면 요약 상태로 돌아오며, 선택과 복귀를 키보드로 수행할 수 있습니다.
  - 접근: 선택 없음·CPU·Memory 세 값의 선택 상태를 표시 저장소가 소유하고 카드 활성화 하나로만 전이시킵니다.
    하단 상세 영역은 선택 여부와 관계없이 항상 자리를 차지하고 선택 전에는 안내를 표시하며,
    내용이 영역보다 길면 그 영역만 스크롤합니다.
    카드는 표준 `Button`으로 두고 선택과 복귀를 수행하는 키보드 단축키를 함께 제공하며,
    단축키의 존재를 카드에 항상 보이는 표시와 카드 접근성 이름에서 확인할 수 있게 하고,
    CPU 상세에는 User·System·Idle, 논리 코어별 사용률, Load Average,
    프로세스별 사용률과 각 프로세스의 네이티브·Rosetta 실행 여부를,
    Memory 상세에는 App·Wired·Compressed·Cached, Swap 사용량과 증가량,
    현재 사용량 순위와 최근 증가량 순위를 둡니다.
    상세의 앱 항목을 펼치면 그 앱의 하위 프로세스가 나타납니다.
  - 검증 조건:
    - 결과: 선택 없음 → CPU → 선택 없음, 선택 없음 → Memory → 선택 없음, CPU → Memory 전이가
      카드 활성화만으로 일어나고 수집 갱신이나 수집 실패는 선택 상태를 바꾸지 않습니다.
      선택 전후로 팝오버 프레임 크기가 같고 상세 영역의 위치가 같습니다.
      CPU 상세에 User·System·Idle, 코어별 사용률(개수가 논리 코어 수와 같음), Load Average,
      프로세스별 사용률과 Rosetta 여부가 모두 나타납니다.
      Memory 상세에 App·Wired·Compressed·Cached, Swap 사용량과 증가량,
      현재 사용량 순위와 최근 증가량 순위가 서로 다른 목록으로 나타납니다.
      시스템 전체 CPU와 프로세스 CPU가 서로 다른 단위 라벨을 가져 화면에서 구분되고,
      여러 코어를 쓰는 프로세스의 100% 초과 값이 잘리지 않고 표시됩니다.
      앱 항목을 펼치면 그 앱으로 묶인 하위 프로세스 목록이 나타납니다.
      macOS 키보드 탐색 설정이 꺼진 기본 환경에서도 단축키만으로 두 카드의 선택과 복귀가 모두 가능하고,
      단축키의 존재가 화면 표시와 카드 접근성 이름에서 확인됩니다.
    - 확인: 선택 상태 전이를 Swift Testing으로 전수 검증합니다.
      이 테스트가 고정하는 것은 "수집 갱신이 선택을 초기화하지 않는다"입니다 —
      선택 상태에서 수집 결과 갱신과 지표 실패를 연속으로 넣고 선택이 유지되는지 단언하며,
      선택을 표시 값과 함께 재생성하도록 되돌리면 이 테스트가 실패해야 합니다.
      단축키 안내 문구와 접근성 이름 포함은 조립 함수 단위 테스트로 단언합니다.
      실제 키 입력과 크기 고정은 XCUITest로 확인합니다 —
      팝오버를 연 뒤 각 카드의 단축키를 눌러 상세가 나타나는지,
      같은 단축키를 다시 눌러 요약 안내로 돌아오는지, 선택 전후 팝오버 프레임 크기가 동일한지를 단언합니다.
      상세 지표 구성은 조립 함수 단위 테스트로 항목 존재와 코어별 값 개수를 단언하고,
      단위 라벨 구분은 시스템 전체와 프로세스 값의 라벨 문자열이 다름을 단언합니다.
      VoiceOver의 실제 낭독은 spec.md 제외 범위이므로 이 Task에서 확인하지 않습니다.
  - 참조: SPEC §5.2, SPEC §5.3, SPEC §5.6, SPEC §5.8, SPEC §5.13,
    ANALYSIS §1 「표시 경계」, ANALYSIS §2 「팝오버 열림과 카드 선택」, ANALYSIS §3 「표시 계약」,
    ANALYSIS §5 DP14, ANALYSIS §5 DP15

- [x] task-011: 지표별 실패의 카드 격리
  - 목적: CPU와 Memory 중 한쪽의 수집이 실패해도 다른 카드와 메뉴바 표시는 계속 동작하고,
    실패한 카드는 마지막 성공 값과 그 시각을 유지한 채 최근 수집이 실패했음을 나타내며 0으로 표시되지 않습니다.
  - 접근: 카드 표시 상태 조립에서 샘플 안의 지표별 성공·실패를 카드별로 갈라
    수집 중·정상·실패 세 경우로 매핑하고, 실패에는 마지막 성공 값과 그 시각을 함께 담습니다.
    중지는 수집 결과에서 유도할 수 없으므로 표시 저장소가 수집 tick과 별개의 진입점으로 일정 중지·재개 전이를 받고,
    코디네이터가 생명주기 store의 전이를 그 진입점에 넣습니다.
    중지를 받으면 두 카드가 마지막 성공 값을 유지한 채 중지가 되고,
    재개 자체는 상태를 바꾸지 않으며 다음에 값이 성립한 tick의 조립 결과가 중지를 대체합니다.
    프로세스 조사 실패는 두 카드의 TOP 5만 실패로 바꾸고 시스템 지표 수치는 그대로 둡니다.
  - 검증 조건:
    - 결과: CPU 지표가 실패한 tick에서 Memory 카드는 그 tick의 값으로 계속 갱신되고 메뉴바 표시 상태도 유지되며,
      반대 방향도 같습니다.
      실패한 카드는 마지막 성공 값과 그 시각을 함께 보여주면서 최근 수집 실패를 나타내고,
      값이 0이나 빈 값으로 바뀌지 않습니다.
      성공 이력이 없는 상태에서의 실패는 수집 중과 구분되어 표시됩니다.
      프로세스 조사가 실패하면 두 카드의 TOP 5만 실패를 나타내고 시스템 지표 수치는 그대로 표시됩니다.
      화면을 볼 수 없어 일정이 멈춘 동안은 실패가 아니라 중지로 표시되고,
      재개 뒤 값이 성립한 첫 tick에서 중지가 풀립니다.
      실패가 카드 선택 상태를 바꾸지 않습니다.
    - 확인: 실패를 주입한 샘플 시퀀스로 조립 함수를 Swift Testing 검증합니다 —
      CPU만 실패, Memory만 실패, 둘 다 실패, 프로세스 조사만 실패를 각각 단언합니다.
      중지는 표시 저장소에 중지 전이를 넣어 두 카드가 마지막 성공 값을 유지한 채 중지가 되는지,
      재개만으로는 상태가 바뀌지 않고 값이 성립한 첫 tick에서 풀리는지를 단언합니다.
      이 테스트들이 고정하는 것은 두 가지입니다 —
      "실패를 0으로 바꾸지 않는다"(실패 tick 뒤 카드 값이 마지막 성공 값과 같고 0이 아님),
      "한 지표의 실패가 다른 카드를 실패로 만들지 않는다"(실패 tick에서 반대편 카드가 정상이고 값이 그 tick의 값과 같음).
      두 단언은 카드별 분기를 하나로 합치면 실패해야 합니다.
      메뉴바 표시가 CPU 실패 tick에서 유지되는 것은 판정 호출 횟수와 마지막 전달 상태로 확인합니다.
  - 참조: SPEC §5.10, ANALYSIS §1 「계산 경계」, ANALYSIS §1 「생명주기 경계」,
    ANALYSIS §2 「실패 경로」, ANALYSIS §3 「표시 계약」,
    ANALYSIS §5 DP2, ANALYSIS §5 DP16

## Section: 실기기 검증과 production 구성

- [x] task-012: 실제 중지·재개에서의 수집 정지와 그래프 빈 구간
  - 목적: 실제 화면 잠금, 디스플레이 슬립, 빠른 사용자 전환에서 새 샘플이 쌓이지 않고,
    복귀 후 팝오버를 열면 중지 구간이 그래프에서 시각에 맞는 자리에 빈 채로 남으며 중지 전후 값이 이어 붙지 않습니다.
  - 접근: 세 상태의 전이와 그때 적용된 두 일정, 두 축의 누적 샘플 수,
    재개 첫 tick이 기준점만 갱신했는지를 `#if DEBUG` 로그(`.notice` 수준, `privacy: .public`)로 남기고
    실제 조작으로 관찰합니다.
  - 검증 조건:
    - 결과: 화면 잠금, 디스플레이 슬립, 빠른 사용자 전환 각각에서 두 일정이 모두 멈추고
      중지 동안 두 축의 누적 샘플 수가 늘지 않습니다.
      복귀하면 중지 이전의 팝오버·전력 조합에 해당하는 두 주기로 재개됩니다.
      재개 첫 tick은 경과 시간이 허용 범위를 넘어 사용률을 만들지 않고 기준점만 갱신하며,
      중지 전 값과 재개 후 값이 하나의 변화량으로 이어 붙지 않습니다.
      복귀 후 팝오버를 열면 중지 구간이 그래프에서 빈 자리로 보이고 전후 점이 선으로 이어지지 않으며,
      마지막 샘플이 오래된 상태에서도 빈 구간이 오른쪽 끝에 붙어 보입니다.
      5초 내외의 짧은 중지·재개를 5회 반복해도 매번 재개되고 중지에 갇히지 않습니다.
      중지·재개 중에도 메뉴바 항목과 팝오버 열림·닫힘은 계속 동작합니다.
    - 확인: macOS 26.5 Apple silicon에서 앱을 실행한 채 사람이 직접 조작해 관찰합니다 —
      화면 잠금과 해제, 디스플레이 슬립과 깨우기, 빠른 사용자 전환과 복귀를 각각 수행합니다.
      세 조작은 자동화할 수 없으므로 사람이 직접 수행하고,
      각 조작마다 로그의 일정 전이와 누적 샘플 수 변화를 기록해 중지·재개 시점을 확인합니다.
      각 조작 뒤에 팝오버를 열어 그래프의 빈 구간 위치를 육안으로 확인하고,
      10분 이상 유지되는 긴 중지 1회와 5초 내외 짧은 중지 5회를 각각 수행합니다.
      긴 중지에서는 복귀 직후 그래프가 전 구간 비어 있는지, 이후 샘플이 오른쪽부터 다시 채워지는지 확인합니다.
  - 참조: SPEC §5.11, SPEC §5.12, ANALYSIS §2 「수집 중지와 재개」, ANALYSIS §4 「테스트 대상」,
    ANALYSIS §5 DP11, ANALYSIS §5 DP12

- [ ] task-013: 시스템 도구와의 정확성 비교
  - 목적: CPU와 Memory의 값과 변화 방향이 정해진 비교 절차에서 macOS 시스템 도구와 일관되고,
    프로세스 CPU 사용률이 코어를 합산한 값으로 표시되어 여러 코어를 쓰는 프로세스가 100%를 넘습니다.
  - 접근: 시스템 전체 CPU, Memory 구성 항목과 Swap은 Activity Monitor와 비교하고,
    프로세스 CPU 사용률과 Resident Memory는 `top`의 `%CPU`·`RSIZE` 열과 비교합니다.
    유휴 상태, 논리 코어 수만큼의 `yes > /dev/null` 부하, 큰 메모리 할당 세 시나리오를 각각 3회 반복하고
    앱과 두 도구를 동시에 띄운 채 같은 시점의 값을 기록합니다.
  - 검증 조건:
    - 결과: 세 시나리오 모두에서 값의 변화 방향이 비교 도구와 일치합니다.
      전체 CPU 사용률은 동시 관측에서 5%p 이내, Memory 구성 항목과 Swap은 10% 이내로 일치하고,
      전체 물리 메모리는 정확히 일치합니다.
      프로세스 순위는 상위 3개 집합이 `top`과 일치합니다.
      논리 코어 수만큼 부하 프로세스를 띄우면 그 프로세스들의 표시값이 100%를 넘고,
      같은 시점의 시스템 전체 CPU 값은 100%를 넘지 않습니다.
      각 시나리오 3회 반복이 모두 위 기준을 만족합니다.
    - 확인: macOS 26.5 Apple silicon에서 앱, Activity Monitor, `top`을 동시에 띄우고
      각 시나리오를 3회씩 관측해 값과 판정 결과를 기록합니다.
      두 도구의 화면 값을 같은 시점에 사람이 읽어야 하므로 자동화하지 않으며,
      기준을 벗어난 항목이 있으면 그 지표와 편차를 기록합니다.
      절대값 오차를 허용하는 근거는 `vm_statistics64` 카운터와 Activity Monitor 항목의 대응이
      공식 문서로 규정된 관계가 아니고 두 도구의 샘플링 시점이 다르기 때문입니다.
  - 참조: SPEC §5.3, ANALYSIS §근거 「추정」, ANALYSIS §2 「프로세스 조사 tick」, ANALYSIS §5 DP13

- [ ] task-014: production 구성과 Sandbox·산출물 유지
  - 목적: macOS 26.5 이상 Apple silicon에서 App Sandbox를 유지한 채 ResourceRunner 앱 하나만 실행하면
    두 카드와 메뉴바 표시, 두 축의 수집 일정이 한 세션 안에서 함께 동작하고,
    빌드 산출물에 Helper나 추가 상주 실행 파일이 포함되지 않습니다.
  - 접근: 코디네이터가 두 sample source, 두 Scheduler, 시스템 이력 저장소와 프로세스 이력 저장소,
    표시 저장소를 각각 한 번 만들어 분석이 정한 단일 방향 흐름으로 연결하고 종료까지 보유하며,
    M1의 자리표시 샘플 타입과 자리표시 source를 제거합니다.
    App Sandbox entitlement, deployment target 26.5, arm64 전용 앱 실행 파일과 단일 애플리케이션 대상 구성을 그대로 두고
    새 실행 대상, 로그인 항목, 외부 package 의존성을 추가하지 않습니다.
  - 검증 조건:
    - 결과: 앱 번들의 `Contents/MacOS`에 ResourceRunner 실행 파일 하나만 있고 로그인 항목·Helper 위치가 비어 있습니다.
      entitlement의 App Sandbox가 유지되고 새 임시 예외가 추가되지 않습니다.
      앱 실행 파일이 arm64 단일 슬라이스이고 세 대상의 deployment target이 26.5입니다.
      프로젝트의 native target 목록과 package 의존성 목록이 M1과 같습니다.
      자리표시 샘플 타입과 자리표시 source 심볼이 소스에 남지 않습니다.
      수집한 값과 프로세스 정보가 파일·`UserDefaults`·네트워크로 나가지 않고 재시작 복원 경로가 없습니다.
      Debug 빌드와 Swift Testing·XCTest 전체가 성공합니다.
      실행 앱 한 세션에서 팝오버의 두 카드 표시, 카드 선택과 복귀,
      실제 CPU 부하에 따른 메뉴바 접근성 이름 전환, 팝오버 열림·닫힘에 따른 수집 주기 변경이 함께 동작합니다.
    - 확인: 격리된 DerivedData로 `xcodebuild` Debug 빌드와 전체 테스트를 실행해 성공을 확인합니다.
      `xcodebuild -showBuildSettings`로 세 대상의 deployment target과 앱 아키텍처 설정을 확인하고,
      빌드된 번들에서 `codesign -d --entitlements`로 App Sandbox 유지,
      `lipo -archs`로 실행 파일 아키텍처,
      `Contents/MacOS`와 `Contents/Library/LoginItems` 목록으로 추가 실행 파일 부재를 확인합니다.
      `project.pbxproj`의 native target·package 목록과 최종 diff를 검사하고,
      자리표시 심볼 제거와 외부 전송 경로 부재는 소스 검색으로 확인합니다.
      실행 앱에서는 부하를 올렸다 내리며 팝오버를 열고 닫아 위 동작이 한 세션에서 함께 성립하는지 통합 관찰합니다.
  - 참조: SPEC §5.1, SPEC §5.2, SPEC §5.6, SPEC §5.8, SPEC §5.9, SPEC §5.14,
    ANALYSIS §1 「수집 경계」, ANALYSIS §4 「기존 코드」,
    ANALYSIS §4 「프로젝트 설정과 외부 경계」
